/**
 * DOM = DOCUMENT OBJECT MODEL
 *       Object{} that represents the page you see in the web browser
 *       and provides you with an Api to interact with it.
 *       web broswer constrcuts the DOM when it loads an HTML document,
 *       and structures all the elements in a tree-like representation
 *       JavaScript can access the DOM to dynamically
 *       change the content, strcuture, and style of a web page.
 */
console.log(document);//displays the html source code
console.dir(document);// displays the properties of the page;

document.title="Document Object Model"